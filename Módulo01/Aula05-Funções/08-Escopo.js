let z = 5;

function soma(x, y) {
    return x + y + z;
}

console.log(soma(5, 3));

// O que está dentro do escopo pode ser usado dentro mas não pode ser usado fora.
// já o que está fora do escopo pode ser usado dentro e fora.