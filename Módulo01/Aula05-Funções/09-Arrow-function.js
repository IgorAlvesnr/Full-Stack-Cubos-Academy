// Declaração de função TRADICIONAL
function soma(x, y) {
    //código
    const resultado = x + y;
    return resultado;
}

//Declaração de função ARROW-FUNCTION
const soma = (x, y) => {
    //código
    const resultado = x + y;
    return resultado;
}