let x = 12;

// x = x + 3; --> resultado 15.
// x += 3; --> resultado também 15, += INCREMENTE a quantidade pedida ao valor de x.
// x++; resultado 13, INCREMENTA apenas 1 ao resultado de x.

// x = x - 3; --> resultado 9.
// x -= 3; --> resultado também 9, -= DECREMENTA a quantidade pedida ao valor de x.
// x--; resuldade 11, x-- DECREMENTA apenas 1 ao resultado de x.

// x = x * 3; --> resultado 36.
// x *= 3; --> resultado também 36, *= INCREMENTE.

// x = x / 3; --> resultado 4.
// x /= 3; --> resultado tabém 4, /= INCREMENTE.

console.log(x);

// ou seja para INCREMENTAR ou DECREMENTAR o valor à variável use o sinal aritimético antes do sinal de igual(recebe).
// não podemos utilizar x** pois esse símbolo é de EXPONENCIAÇÃO.
// não podemos usar o // pois esse símbolo permite adicionarmos comentários no meio do código.