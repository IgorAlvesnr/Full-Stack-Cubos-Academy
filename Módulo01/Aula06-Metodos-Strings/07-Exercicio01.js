const numero = "97.50";

const variavelAlterada = numero.replace(".", ",");

console.log(numero);
console.log(variavelAlterada);

/* Muito importante lembrando que tem que ser uma variável do tipo string. mesmo se for números. */