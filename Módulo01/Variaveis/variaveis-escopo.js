{
    const idade = 30;
    {
        console.log(idade);
    }
}

console.log(idade);

/*ESCOPO ou seja { }: se refere a região do código onde uma variável pode ser acessada. 
Variaveis criadas dentro do ESCOPO só podem ser lidas se estivem dentro do ESCOPO, se não estiver dentro
dará erro e aparecerá ReferenceError: idade is not defined"*/