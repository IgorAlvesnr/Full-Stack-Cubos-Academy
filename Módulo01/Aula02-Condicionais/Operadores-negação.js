const temIngresso = false;

if (!temIngresso) { // Quando se coloca a exclacão antes da variável trocasse o valor booleano dela.
    console.log("Pessoa sem ingresso")
}
console.log(`Variavel temIngresso: ${temIngresso}`);

/* Retorna o oposto do valor da expressão(temIngresso). Ou seja, retorna verdadeiro caso a expressão seja falso,
e retorna falso caso seja verdadeiro.*/