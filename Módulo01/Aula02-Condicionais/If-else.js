const idade = 10;

if (idade >= 18) {
    // é executado caso a condição for verdadeira.
    console.log("É maior de idade");
} else {
    // é executado caso a consição seja falsa.
    console.log("É menor de idade.")
}
console.log("Fim do código");

/* é possível funcionar o código if-else mesmo sem as chaves; mesmo não sendo muito aconselhavel porque sem as 
chaves só é possível ter uma linha dentro do if-else. */