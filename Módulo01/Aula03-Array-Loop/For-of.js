const nomes = ['jose', 'joao', 'maria', 'joana', 'andre']

for (let nome of nomes) {
    console.log(nome)
}

/* conceito: for(let item of array){
    o código entre as chaves se repetirá uma vez para cada item do array
}  */
