const nomesDeEstudantes = ['jose', 'joao', 'maria', 'joana', 'andre'];

//for (let i = 0; i < 5; i++) { --- a mesma coisa que a linha de baixo
for (let i = 0; i < nomesDeEstudantes.length; i++) {
    console.log(nomesDeEstudantes[i]);
}